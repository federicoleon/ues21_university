class UrlMappings {

    static mappings = {

        "/"(controller: "login", action: "login")
        "/"(controller: "login", action: "login")

        "/management/logout"(controller: "login", action: "logout")

        "/management/main"(controller: "management", action: "main")
    }
}
